pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
		sh '''
			#!/bin/bash
			cd /var/lib/jenkinsfile/workspace
			git clone https://github.com/sandysanjeev2/test1.git
			cd test1
			make
		'''
            }
        }
    }
}
